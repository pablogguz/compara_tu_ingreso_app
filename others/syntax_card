 <div 
        style={{ 
          height: '100%', 
          width: '100%', 
          display: 'flex', 
          flexDirection: 'column', 
          alignItems: 'center', 
          justifyContent: 'center', 
          backgroundColor: 'white',
          position: 'relative',
        }} 
      >
        {/* Top left URL with icon */}
        <div
          style={{
            position: 'absolute',
            top: 48,
            left: 64,
            display: 'flex',
            alignItems: 'center',
            gap: 12,
          }}
        >
          {/* Small icon */}
          <svg viewBox="0 0 100 100" width="40" height="40">
            <rect x="0" y="0" width="100" height="100" rx="15" fill="#58a2ec" />
            <path 
              d="M10 80 Q35 20 50 50 Q65 80 90 80" 
              fill="none" 
              stroke="#ffffff" 
              strokeWidth="5" 
              strokeLinecap="round" 
            />
          </svg>
          
          {/* URL with gradient */}
          <div
            style={{
              fontSize: 26,
              fontFamily: 'Inter',
              fontWeight: 800,
              background: 'linear-gradient(135deg, #0f172a, #58a2ec)',
              backgroundClip: 'text',
              color: 'transparent',
              letterSpacing: '-0.02em',
            }}
          >
            comparatuingreso.es
          </div>
        </div>

        {/* Main heading - matching landing page with gradient */}
        <div 
          style={{ 
            fontSize: 48, 
              marginTop: 24, 
            fontFamily: 'Inter',
            fontWeight: 900, 
            letterSpacing: '-0.04em',
            textAlign: 'center',
            maxWidth: '900px',
            lineHeight: 1.3,
            background: 'linear-gradient(135deg, #0f172a, #58a2ec)',
            backgroundClip: 'text',
            color: 'transparent',
          }} 
        > 
          Descubre tu posición en la distribución de ingresos
        </div> 
       
        {/* Subtitle - matching landing page */}
        <div 
          style={{ 
            marginTop: 24, 
            fontSize: 24,
            fontFamily: 'Inter, sans-serif',
            color: '#64748b', 
            lineHeight: 1.5, 
            textAlign: 'center',
            maxWidth: '820px',
          }} 
        > 
          Utilizando datos de declaraciones de IRPF
        </div> 
      </div>